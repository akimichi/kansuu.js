"use strict";

var expect = require('expect.js');
var __ = require('../lib/kansuu.js');
var base = require('../lib/kansuu-base.js');
var math = require('../lib/kansuu-math.js');

describe("'combinator' example", () => {
  // it('temp', (next) => {
  // 	var data = {
  // 	  temp: 24,
  // 	  time: new Date("2013/2/15")
  // 	};
  // 	var judge = (data) => {
  // 	  var temp = __.objects.get('temp')(data);
  // 	  return __.and(math.isMoreThan(20))(math.isLessThan(30));
  // 	};
  //   expect(
  //     judge(data)
  //   ).to.eql(
  // 	  true
  // 	)
  //   next();
  // });
});
